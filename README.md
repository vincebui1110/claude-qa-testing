# Claude QA Testing

Automated Shopify app testing with Claude Code + Puppeteer.

## Setup

1. Copy files vào `~/.claude/` theo structure
2. Sửa `ENV.md` cho máy hiện tại
3. `npm install -g puppeteer-core`
